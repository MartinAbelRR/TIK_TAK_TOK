const victoria = (matrix: boolean[][]): void => {
    for (let i = 0; i < matrix.length; i++) {
      for (let j = 0; j < matrix.length; j++) {
        const element = matrix[i][j];
        console.log(element);        
      }          
    }
}

